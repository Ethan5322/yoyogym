name: CI

# Runs on every push and pull request: install, unit tests, production build.
# Protects all gym deployments — a broken change fails here before it ships.
on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Unit tests
        run: npm test

      - name: Validate the Obsidian vault
        # The vault is the project's memory of record (CLAUDE.md 26). A broken
        # link or a stale note is how a later session reaches a confident wrong
        # answer, so drift fails the build rather than waiting to be noticed.
        run: npm run docs:validate

      - name: Production build
        run: npm run build
        env:
          # Build needs no secrets (all server secrets are runtime env on Vercel).
          CI: 'true'
