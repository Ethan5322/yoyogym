---
aliases: ["Decision Log", "Decisions"]
tags: [decisions, log]
stage: "Stage 2"
status: active
updated: 2026-09-22
---

# 18 — Decision Log

Append-only. One row per decision, newest first. A decision enters this log **only after the user
has approved it**. Proposals live in [[17 - Open Questions]] until then.

Per `CLAUDE.md` §29, an approved decision recorded here outranks repository behaviour for *future*
work — but never rewrites what the code actually does today ([[01 - Existing Yoyo Gym Audit]]).

| # | Date | Decision | Rationale | Affects |
|---|---|---|---|---|
| D-155 | 2026-09-24 | **Stage 8 opened (user, 2026-09-24): the app gets its OWN member screens; registration stays on the gym's web flow.** Until now, picking a gym opened the gym's website inside the app. The member's everyday screens are now native to the app, in `apps/mobile/www/member.js`: sign-in with the same number and phone (§9), Home (status first, then one-tap check-in), an offline membership Card, Classes (only if the plan includes them) and Profile. They call **the same `/api/member/*` endpoints as the website**, so every rule is still enforced once, on the server. **Registration is NOT reimplemented.** The 38-step flow with the PAR-Q stays on the gym's own web screens, and "Join" opens it, because a second implementation would be a second place to handle health answers wrongly. **Two prerequisites were real bugs:** there were no CORS headers, so on a phone the app could not read any reply from its own server (fixed for the two app origins, JSON APIs only, never with credentials); and `iosScheme: 'https'` is impossible on WKWebView, so the iPhone app could not have loaded. **Stored on the phone:** the session and a cached card, in the app's private storage, per gym. A hardware-backed secure store is a later step. Reversible: the website member area still exists, and Profile links to it | The user asked for a premium, professional store app and opened Stage 8 | `apps/mobile/www/member.js`, `apps/mobile/www/index.html`, `apps/mobile/www/app.js`, `shared/cors.js`, `apps/mobile/capacitor.config.ts` |
| D-154 | 2026-09-23 | **The panel got a front page, and stopped showing machine output to people.** An audit found the reachability clean (the only unlinked routes are Paystack's callback and webhook, which are not pages) but three real faults. **(1) Ten places rendered `2026-09-23T14:32:07.481Z`** — a serialisation format, not a date, charging the reader a translation on every line. `platform/format.js` now gives recent events a relative form and older ones an absolute one with a **word for the month**, because 09/10 is September in one country and October in another and this product is worldwide (D-125). **(2) Signing in landed on the applications queue** — one list, chosen because it was built first. `/platform/home` now opens on what is waiting for a decision, ordered by **what it costs to ignore**, and says plainly when nothing needs attention. Every figure links to the thing it counts, because a number nobody can act on is decoration and decoration on an operations screen reads as information. One failing figure cannot take the page down. **Two of my own bugs surfaced writing it**: `money(null)` rendered `ZAR 0.00` — the **third appearance of `Number(null) === 0`** in this codebase — and `until()` called a trial ending tonight "tomorrow" by rounding elapsed hours instead of comparing calendar days, wrong on exactly the day somebody must act | The user asked for the panel to be corporate-grade with no dead text | `platform/format.js`, `platform/views.js`, `platform/router.js` |
| D-153 | 2026-09-23 | **`db/schema.sql` is now IMPORTED as a generated module, not read from disk.** The previous fix — `outputFileTracingIncludes` in `vercel.json` — **was rejected by Vercel**: that is a `next.config.js` option, and vercel.json refuses unknown keys. ⚠️ **My own config test passed it**, because I had put both tracing keys in the ALLOWED list from memory — **an allow list built from assumption cannot catch an assumption**, and that is the second deploy this file has broken. Both keys are out of the list now. The real fix: `npm run build:schema` generates `db/schema.sql.js`, and an **import is traced by the bundler where a `readFileSync` is not**. `db/schema.sql` stays the single source of truth; the build regenerates the module so it cannot go stale, and a test compares the two byte for byte | The deploy was rejected, and the reason was a key I had assumed was valid | `db/schema.sql.js`, `scripts/build-schema-module.mjs`, `platform/schema-runner.js`, `vercel.json` |
| D-152 | 2026-09-23 | **Two more provisioning faults found by auditing the path before running it.** (1) `migration_runs.checksum` is NOT NULL and provisioning passed `options.schemaChecksum ?? null`, so a run would have **created the schema, applied it, seeded it, saved two rows — and then failed on a constraint at step six of seven**, leaving a real schema the registry only half knows about. It now **refuses before step one**: a missing checksum also means `db/schema.sql` could not be read, so there is nothing to apply either. Failing early costs nothing; failing at step six costs an orphan. (2) **`db/schema.sql` was not bundled into the Vercel function at all** — it is read with `readFileSync` at runtime, which Vercel's tracing does not follow, so `applySchema` would have had nothing to apply in production. `outputFileTracingIncludes` now ships it with `api/platform/**`. Separately verified: the schema rewrite is clean — **24 of 24 tables**, no reference to the original `gym` schema left behind | Auditing the provisioner rather than discovering it on a real gym | `platform/provisioning.js`, `vercel.json` |
| D-151 | 2026-09-23 | 🔴 **`exposeSchema` would have TAKEN THE WHOLE PLATFORM DOWN on the first gym ever provisioned.** It read the exposed-schema list from `pg_settings`, which shows the CURRENT SESSION's value — and provisioning runs as `postgres` through the Management API, where that setting is unset. So the `coalesce` would fall back to `'public, graphql_public'` and the next statement would overwrite the `authenticator` role with **that plus the new gym**, dropping `gym` and `platform` from the list. KOM stops serving, the admin panel stops answering, and the only symptom is PGRST106 on everything at once. The list actually lives in `pg_roles.rolconfig` — **which we only learned by debugging it by hand hours earlier**, when the Supabase dashboard silently failed to write it. Now read from `rolconfig`, still appended and never replaced, and **both** reload signals are sent in order: `reload config` for which schemas, then `reload schema` for what is inside them. Four tests, checking the SQL with comments stripped so the prose explaining the bug cannot satisfy the test for the fix | Found by checking the provisioner before running it for real, not by running it | `platform/schema-runner.js` |
| D-150 | 2026-09-23 | **Android project scaffolded, and two things the config only INTENDED were made real.** `capacitor.config.ts` carried a comment saying a device backup must not copy a live session off the phone — and the generated manifest shipped `allowBackup="true"`, Android's default and the opposite of the stated intent. **A comment configures nothing.** Now `false`, with a test. The **CAMERA permission is declared explicitly** rather than assumed from the plugin, because a permission that is merely assumed fails at the gym door instead of at build time; `uses-feature required="false"` so a phone without a camera can still install the app, which is correct since search by name works. Tests now compare the native `capacitor.config.json` against `shell.config.json`, so a break anywhere in shell.config → capacitor.config.ts → `cap sync` → the app is caught before an install rather than after. `capacitor.config.ts` also lost its `__dirname`, which does not exist under the ES-module loader Capacitor used | Stage 8 scaffolding | `apps/mobile/android/`, `apps/mobile/capacitor.config.ts` |
| D-149 | 2026-09-23 | **The app shell points at a VERCEL URL, not an unregistered domain.** No domain is bought yet, and the choice was between `yoyogyms.com` — a name somebody else can buy, sitting on the list that decides where a member types their ID number — and a deployment address that is honest about being temporary. The cost is **written into the config rather than left to be discovered**: an APK built against this address keeps it, so moving to a real domain means a new build and, once on a store, a new review and rollout. A test asserts that cost is documented | The user has no domain and wants to test now | `apps/mobile/shell.config.json` |
| D-148 | 2026-09-23 | **QR scanning built, and the payload rules live in `shared/qr-payload.js` — GENERATED into the app, never copied.** `npm run mobile:configure` transforms the shared module into `www/qr-payload.js`, because a hand-written second copy would drift and **the drift would be silent**: the app accepting a payload the platform refuses, or refusing one it should take. Four screen states are designed rather than left to fail — scanning, camera refused, not-a-Yoyo-code, and a code carrying a secret — and the last two read differently on purpose, since one is a mistake and the other is a warning. Controls sit in the bottom third because a hand is already there and the camera needs the top | `CLAUDE.md` §14, D-036, and the mobile design skill the user asked for | `shared/qr-payload.js`, `apps/mobile/www/` |
| D-147 | 2026-09-23 | **A QR payload is an https URL (`/g/<slug>`), and anything carrying a secret is REFUSED before it is read.** A URL because §14 needs one code to open the app if installed, a web page if not, the right store, and the right gym afterwards — only a real https URL does all four, and App Links can claim it later without reprinting a code. `FORBIDDEN_PARAMS` rejects `code`, `verification_code`, `token`, `password`, `secret`, `key`, `pin`: `POST /api/document` accepts membership number plus verification code with no session, so a QR carrying that pair would be **a document-access token anybody could photograph off a wall**. ✅ **Checked and CLEARED: the existing `/p/:type/:key` codes carry the MEMBERSHIP NUMBER, not the verification code** — the risk vault/09 flagged as unsolved does not exist in the code. A member QR names a member and **never signs them in** (§14) | The vault flagged the risk; the code turned out to be safe, and now the rule is enforced | `shared/qr-payload.js` |
| D-146 | 2026-09-23 | **The existing gym becomes tenant #1, "KOM", WITHOUT MOVING ANY DATA.** Its 24 tables of real members, payments and check-ins stay in the schema `gym` exactly where they are; `gym_connections.schema_name` is simply set to `gym`, and the resolver reads that column rather than assuming a naming pattern. Every later gym gets `gym_<slug>`; **KOM keeps `gym` because it was there first**, and renaming a live schema to make the naming tidy would be risk taken for no reason. The migration is `on conflict do nothing` throughout, alters no gym table, and reads `gym.members` only to count them in its final check | The user's instruction: existing member records are one of the tenant gyms | `platform/migrations/2026-09-23-kom-first-tenant.sql` |
| D-145 | 2026-09-23 | 🔴 **`gym_connections.supabase_project_ref` and `supabase_url` were NOT NULL, and schema-per-gym provisioning writes null to both.** Left as they were, **every real provision would have failed on a constraint violation** — and only against a live database, because the dry run inserts nothing and all 530 tests use stubs. They were correct under D-016 (a project per gym) and were never revisited when D-096 replaced it. Now nullable; `schema_name` stays NOT NULL because **it is the isolation boundary**. Filling the two nullable columns in is still what moves one gym to its own project later, which `server/lib/tenancy.js` already supports | Found while making the existing gym a tenant | `platform/schema.sql` |
| D-144 | 2026-09-22 | 🔴 **THE SUBSCRIPTION COULD NOT TAKE A SINGLE RAND, and now can.** Three gaps found by asking how an owner actually pays: **no pay route at all**, no callback, and — the worst — `chargeAuthorization` was called with `authorizationCode: null` **unconditionally**, so every renewal would have failed. There was a first payment and then silence, which is not a subscription. `platform_subscriptions` now stores `paystack_auth_code` (a token authorising future charges on that card; **not a card number and it cannot read one**), plus brand and last4 for the owner's own screen. Two rules carry the flow: **the amount is read from the PLAN, never from the request** (a price arriving from a browser is a price the payer chose), and **the callback is never believed** — anyone can type that URL, so `verifyTransaction()` asks Paystack server-to-server and an amount that disagrees with the invoice is refused and audited. The invoice is raised **before** the payment page, because an abandoned page leaves a question somebody can answer while a payment with no invoice is money with no reason. **On the web, never in the app** (D-060): stores take 15–30% of an in-app digital subscription against Paystack's ~3%, and the app still has no purchase route | The user confirmed gym owners pay online by subscription | `platform/checkout.js`, `platform/schema.sql`, `platform/deps.js`, `platform/billing-runner.js` |
| D-143 | 2026-09-22 | **First-run setup built (`/platform/setup`), the last thing between the SQL and a working panel.** The seed creates the owner with **no password and no 2FA on purpose** — a password typed into a SQL editor ends up in the clipboard, the query history and probably a screenshot. Two guards, both required: a **`PLATFORM_SETUP_TOKEN` from the environment** (whoever can set one already controls the deployment, so it is a real trust root), and **the account having no password yet**. The second is the one that matters: it makes setup **a one-time act that closes behind itself**, so even somebody holding the token cannot use it to take over a working account. **The authenticator is PROVEN before 2FA is switched on** — enabling it without checking would lock the only owner account out of the platform on its first day, with no second account to fix it from. Recovery codes are shown **once**; only hashes are stored. The done page says to remove the token afterwards | The SQL was run and the seeded account could not sign in | `platform/setup.js`, `platform/views.js`, `platform/router.js` |
| D-142 | 2026-09-22 | **D-130 ANSWERED (user, 2026-09-22): per-gym COUNTS ONLY, never names.** §16 asked for aggregate gym statistics and D-044 forbids platform staff seeing member data; the user resolved it in favour of numbers without identities. **The safety is STRUCTURAL, not a promise:** every count uses PostgREST `head: true`, so the database returns the number in a header and **no rows at all** — there is no array of members to log, serialise or return from a function that later grows a new caller. A reviewer does not have to audit what happens to the rows, because there are none. The single column read by name is `created_at`, and the list is frozen in the module so no caller can widen it. **Every read is audited** with the staff member's id — reaching into a gym's own schema is something the platform should have to account for even when all it takes is a number. A test caught the same `Number(null) === 0` trap as the coordinate bug earlier in this session: an **unreachable** gym would have been reported as "0 members, comfortably within its plan", a confident answer about a gym nobody could see | Two of the project's own rules disagreed; the user chose | `platform/stats.js`, `platform/deps.js`, `platform/views.js` |
| D-141 | 2026-09-22 | **Platform security alerts built (`CLAUDE.md` §16, "controls platform-level security").** The audit log recorded everything and nothing read it for PATTERNS, so a hundred failed sign-ins and one looked identical to whoever scrolled past. `platform/alerts.js` reads it for six: repeated failed sign-ins **for one account** (spread across many accounts is a busy morning, not an attack), staff refused for missing 2FA, **unusual volumes of identity-document access** (the insider-risk signal — these are strangers' ID scans), uploads claiming a path they were not given, payments matching no invoice, and failed provisioning where an approved owner is waiting. **Every alert states its INNOCENT EXPLANATION beside it**, because most of these usually are innocent and a screen that does not say so trains people first to panic and then to stop reading. **Nothing locks, suspends or sends** — acting automatically would lock out the person doing their job on the day they were busiest. Thresholds are named constants, deliberately generous. Scoped to the PLATFORM: sign-ins, documents, provisioning — **never a gym's members**, which the platform does not see (D-044) and the screen says so | The last unbuilt §16 item after a boundary check confirmed the main panel never touches gym-admin territory | `platform/alerts.js`, `platform/views.js`, `platform/router.js` |
| D-140 | 2026-09-22 | **Document review rebuilt for spotting forgeries, before the SQL is run.** The old screen **redirected out of the panel** to a signed storage URL: the reviewer left, downloaded a file, opened it elsewhere, and had **nothing beside it to compare against**. Judging a forged registration certificate means comparing the document to what the applicant CLAIMED, and that is impossible on two screens. Now the document renders **inline**, next to the applicant's own claims, the facts about the bytes, and every other application this exact file appears on. **`platform/forensics.js` reads magic bytes**, so an executable or a zip renamed `.pdf` is caught — the upload check only ever saw the browser's declared type. **The SHA-256 is computed SERVER-SIDE from the bytes in storage**, never from the uploader: a client-supplied hash is worthless for duplicate detection because anyone reusing a document would simply send a different number. It is cached on the row, so cost is bounded by review volume, not upload volume. **Nothing here decides anything** — every flag is a fact for a person to weigh, because auto-rejecting on a heuristic turns away real gyms with old scanners. The signed URL moved to `/documents/:id/file` so it never appears in the page source, the browser cache or a screenshot. **All 25 panel destinations verified to resolve** — no broken buttons | The user asked for the panel to be checked and finished before running the SQL | `platform/forensics.js`, `platform/views.js`, `platform/router.js`, `platform/deps.js` |
| D-139 | 2026-09-22 | **The browser's half of gym resolution: `/g/<slug>/…` routes and `src/lib/gym.js`.** The app now has somewhere to send a member. The same seam principle as `getSupabase()` on the server: **one function** — `apiFetch()` — makes every SPA request, so adding `X-Gym-Slug` there covers all of them and no page changed. The gym is captured **before `loadBranding()`**, because branding is itself a gym-scoped call and capturing after it would fetch the wrong gym's name and colours. Held in **sessionStorage, not localStorage**: a tab is the right lifetime for "which gym am I looking at", and localStorage on a shared phone would leave one gym selected for the next person. The header is a **HINT, never a credential** — an authenticated request takes its gym from the signed token and a disagreeing header is refused 403 (D-137), so editing it reaches nobody else's data. The `/g/:slug/*` routes render the **SAME screens** as `/register` and `/member`: one registration flow, not two. Sign-out forgets the gym (a reception computer is shared); an expired token does not, since that person is still standing in the same gym | The app linked to routes that did not exist | `src/lib/gym.js`, `src/lib/api.js`, `src/App.jsx`, `src/lib/auth.jsx` |
| D-138 | 2026-09-22 | **D-041 REFINED, not superseded (user, 2026-09-22): the member picks their gym FIRST; searching across gyms is the BACKUP.** The user described a sign-in that finds the member's gym from their details, which needs the platform-level member index D-041 ruled out. Rather than silently override an approved decision, the conflict was raised and the user chose the middle path. **The default is unchanged** — pick a gym, then sign in with membership number + phone exactly as today, protected login untouched — **plus** an "I don't remember which gym I joined" route. That route **names a gym and never signs anybody in**. `platform.member_directory` holds **one keyed digest and one gym id**: no name, no readable phone, no membership number. It is an **HMAC with a server-side key, not a plain hash**, because a membership number is ~1e6 possibilities and a phone is knowable — a plain-hash table plus somebody's phone would reveal which gym they attend in seconds. Rate limited, one generic message for every failure, and a member of two gyms is **asked, never guessed at**. Phone normalisation compares the **last nine digits** after a test caught that `+27 82 123 4567` and `0821234567` — one phone — were being treated as two different people | An approved decision and a new instruction disagreed; the user settled it | `platform/member-directory.js`, `platform/schema.sql`, `platform/api.js` |
| D-137 | 2026-09-22 | 🟢 **Q-47 ANSWERED AND BUILT: the gym travels in the TOKEN. The third branch of the architecture is now connected.** `server/lib/gymcontext.js` supplies the slug that `resolveGym()`/`runWithGym()` have waited for since Stage 4, and all five gym routers run their handler inside it. **The gym is a signed claim, not a header**, because under D-096 every gym shares one `JWT_SECRET` — a token from gym A verifies at gym B, so any client-chosen slug would have let a member of A walk into B, with the resolver working perfectly throughout. A header is accepted only for PUBLIC requests (no token, nothing private), and where it **disagrees** with a token it is **refused with 403**, never silently resolved either way. **Entirely backward compatible:** existing tokens carry no gym claim and existing deployments send no header, both of which mean single-gym mode — no query, no cost, nobody logged out, asserted by the first two tests. Login stamps the gym at issue time. The registry lookup is cached 30s (a QR on a wall is scanned all day) but **secrets never are**, so a rotated key takes effect on the next request | The architecture the user drew: "10,000+ gyms, each connected separately" | `server/lib/gymcontext.js`, `server/lib/tenancy-deps.js`, `api/*`, `server/lib/auth.js`, `server/lib/memberauth.js` |
| D-136 | 2026-09-22 | 🔴 **Q-47 FOUND: the tenancy seam is built, tested and INERT.** `resolveGym()` and `runWithGym()` exist and `tests/isolation.test.js` proves them; `getSupabase()` checks `currentGym()` first, so the seam is right. **Nothing calls any of it** — zero call sites outside the module — so `currentGym()` is always null and every request falls through to single-gym mode. `/g/:slug` does not exist either. Wiring it needs a decision first: under D-096 all gyms share one `JWT_SECRET`, so a **client-supplied gym slug would let a member of gym A present their token against gym B** — exactly the failure the Stage 5 gate forbids, and one the resolver's own tests cannot catch because the resolver would be doing its job. **Recommended: the gym travels in the TOKEN for authenticated requests, path/header only for public ones.** Not implemented; it changes `signToken` (protected surface) and it is a security decision | Found by following the member's path end to end rather than trusting each piece | [[17 - Open Questions]] §0.5 |
| D-135 | 2026-09-22 | **Stage 8 opened; app shell built as a Capacitor wrapper over the server's own screens** (D-038, D-053). The `mobile-app-ui-design` skill was installed as §2 requires. The app **reimplements nothing**: a second implementation of member registration is a second place for health answers to be mishandled and a second set of rules to keep in step. **One deliberate departure from the example studied:** that shell asks each merchant for a server address because each has their own deployment; Yoyo Gyms is ONE deployment serving every gym (D-096), so the address is fixed and what varies is WHICH GYM, resolved by slug (D-036). Asking a member for a server address would be asking them something they cannot know. `shell.config.json` is the single source of truth for the allow list, feeding both `allowNavigation` and the entry screen's validation so they cannot drift. **`allowedHosts` ships EMPTY and the generator REFUSES to build** — yoyogyms.com is not registered, and an unregistered domain in an allow list is a name somebody else can buy, on the list that decides where a member types their ID number | Proven pattern, adapted where the two products genuinely differ | `apps/mobile/`, `scripts/mobile/configure-shell.mjs` |
| D-134 | 2026-09-22 | **JSON API built for the app (`platform/api.js`), and it shares the backend rather than mirroring it.** Every route calls the **same injected dependency** its HTML counterpart calls — same login rule (staff need 2FA, owners do not, D-119), same application validation, same document ownership and path checks. The risk of a second surface is that the app quietly becomes **the weaker door**, so the tests assert the shared rules on the API side specifically. Auth is the **same signed platform token over a different transport**: `readAnySession()` accepts a cookie (browser) or `Authorization: Bearer` (app). Two transports, one identity — not two auth systems to keep in step. Responses are **shaped, never echoed rows**: the dashboard row contains `schema_name` and the API response must never, which a test asserts | The app cannot consume HTML; without this "same backend" was not true | `platform/api.js`, `platform/http.js` |
| D-133 | 2026-09-22 | **SURFACE BOUNDARY CORRECTED by the user (2026-09-22): the WEBSITE IS ONLY THE MAIN ADMIN PANEL.** Gym owners and gym members use **the app**; Yoyo staff use **the website**; both run on one backend. I had built owner and member screens (`/apply`, `/my-gym`, `/activate`, `/find`) as website pages, which put them on the wrong surface. **They are kept, but re-labelled as the WEB FALLBACK `CLAUDE.md` §14 already requires** — an activation email link and a scanned QR must work for someone who has not installed the app — and each now has a JSON twin that is the primary path. They stay out of the staff navigation. **No app route approves an application or suspends a gym**, so the most consequential actions have exactly one door | User correction; §14 already required a web fallback, so the pages were right and their framing was wrong | `platform/router.js`, `platform/api.js` |
| D-132 | 2026-09-22 | **Platform notifications are now SENT, not only recorded.** `notify()` wrote a row to the audit log and stopped, so an owner was never told their trial was ending, their card had failed, or their gym had been suspended. Now sent by email **and** recorded — recorded even when sending fails, so "I was never told" can be answered from the log. The wording is load-bearing: a failed-payment email says **the gym is still open** (the grace window is meaningless if the email reads like a cut-off) and a suspension email says **nothing has been deleted** | Written but unsent notifications are worse than none — they look done | `platform/email.js`, `platform/deps.js` |
| D-131 | 2026-09-22 | 🔴 **BLOCKER FOUND AND FIXED: the activation link was generated and thrown away.** Approving an application provisioned a real gym database and issued an activation link — and nothing emailed it, nothing displayed it, and the router discarded the return value. **The owner could never activate, so the gym could never open. Onboarding was broken at the final step.** Fixed twice over: `platform/email.js` sends it (Brevo, its own sender — D-081, and a different voice from a gym's own mail), AND if sending fails for any reason the reviewer is **handed the link and code on screen**, so the chain completes with no mail provider configured at all. The handover page is shown **once** and states so, because only hashes are stored and that is what the activation design rests on. **The link never goes in a URL** — query strings land in server logs, browser history and the next request's referer | Found by tracing the chain end to end rather than trusting that each piece existed | `platform/email.js`, `platform/router.js`, `platform/views.js`, `platform/deps.js` |
| D-130 | 2026-09-22 | **Per-gym member statistics are NOT built, and there is a design tension behind it.** `CLAUDE.md` §16 asks for "aggregate gym statistics", and D-044 says support staff must never see member data. Reading member counts means the platform reaching into a gym's schema — technically trivial under D-096, and **exactly the boundary the POPIA position rests on** (D-014: the gym is responsible party for its members). Counting is not reading names, so a narrow aggregate query is defensible, but that is **a decision, not an implementation detail**. Left unbuilt and raised rather than quietly building the boundary crossing | Two approved decisions point in opposite directions | [[17 - Open Questions]], [[14 - Security and Privacy]] |
| D-129 | 2026-09-22 | **One gym can be moved to another plan** (`POST /platform/registry/:id/plan`, `subscription.manage`). The key is validated against the plans we actually sell **before** it is written: an unknown `plan_key` would leave the gym with **no entitlements at all**, because gating fails closed (D-102) — every feature would switch off at once and nobody would know why. The plan is written to **both** `gyms.plan_key` (what entitlement resolution reads) **and** the live subscription (what billing reads); writing one without the other gives a gym one plan's features and another plan's invoice. The screen says plainly that **a downgrade never deletes members** | Every gym eventually asks to upgrade; without this it meant editing the database by hand | `platform/router.js`, `platform/deps.js` |
| D-128 | 2026-09-22 | **The other half of the platform panel built** (`CLAUDE.md` §16), after the user challenged whether it was actually finished. It was not — the applications and registry halves existed and the operating half did not. Added: **plans and prices** (the screen the platform cannot run without — `price_cents` was NULL and nothing could set it, so **billing skipped every plan and charged nobody**), the **audit log reader** (everything wrote to it, nothing could read it, and it is the POPIA record of who opened whose ID), **gym and owner search** (a list capped at 500 with no search is not a registry at 10,000 gyms), **owner deactivation** (a compromised owner account had no off switch short of a SQL console), and a **finance summary** that names any unpriced plan out loud. Prices are entered in **rands** and stored in **cents**, converted in exactly one place | The user asked whether the panel was really built; it was half built | `platform/router.js`, `platform/views.js`, `platform/deps.js` |
| D-127 | 2026-09-22 | **D-123 fixes 1 and 2 APPLIED (user, 2026-09-22).** `/api/document` now returns an explicit 18-column list instead of `select('*')` — **no `id_number`, no `passport_number`, no `face_descriptor`, no `arcface_embedding`/`arcface_templates`, no `photo_url`** — and is rate limited at 10/min in its own bucket. The list was **measured** from `generateMembershipPdf.js` and `idcard.js`, and a test re-derives it from those files, so narrowing it cannot silently break a member's card. `parq_responses` deliberately still selects everything: every column of it is the member's own PAR-Q answers, which is exactly what the PDF prints. **Option 3 (session-binding) remains open** — it changes the member flow and is a product decision | Protected surface, changed on explicit approval | `server/handlers/public/document.js` |
| D-126 | 2026-09-22 | **F-1 FIXED (user, 2026-09-22).** `server/lib/auth.js` `verifyToken()` now refuses a token carrying a FOREIGN audience. Written as "reject a foreign `aud`" rather than "require `aud === 'admin'`" **on purpose**: admin tokens in the wild carry no `aud` at all, so a positive check would have logged out every signed-in staff member mid-shift. Once every live session has expired (8h), `signToken` can start setting `audience: 'admin'` and this can become a positive check — **a separate, later change**. 8 tests, two of which exist purely to prove existing sessions still work | Approved fix to protected surface | `server/lib/auth.js` |
| D-124 | 2026-09-22 | **Q-46 ANSWERED: verifying the owner's email OPENS THE GYM (user, 2026-09-22).** D-049 (first payment activates) is **superseded**. Under it a trialing gym was refused by tenancy resolution, so "30 days free" was a promise the system could not keep — the owner signed up, was told they had 30 days, and found a gym that would not open. Activation now sets the gym `active`; the trial runs alongside it. `activatesGym` stays an argument rather than a constant, because reactivating a suspended owner's account is a real case for activating the person without opening the gym | User decision, option A | `platform/activation.js`, `platform/router.js` |
| D-125 | 2026-09-22 | **Yoyo Gyms is WORLDWIDE, not South African (user, 2026-09-22).** Resolves the `CLAUDE.md` §30 open item "jurisdictions beyond South Africa". **"Local" is a property of the GYM, not of the software**: a gym in Addis Ababa asks Ethiopians for their national ID and everyone else for a passport, exactly as a Cape Town gym asks for an SA ID. `HOME_COUNTRY = 'ZA'` stops being a global truth and becomes a **fallback**; `homeCountryFor(gym.country)` is the real answer. **Only South Africa keeps a strict format check**, because the SA ID is the only document this codebase knows how to validate — rejecting a real Kenyan ID for not being 13 digits would make the gym unusable in Kenya. A well-formed but unlisted country code is honoured: the 50-country list is a convenience for dial codes and currencies, **not a statement about which countries may have gyms**. Much of this was ALREADY built — nationality already chose the document, phone and currency already adapted; the single hard-coded constant was the whole gap | User instruction; the existing design had anticipated it | `shared/countries.js`, `src/chatbot/flow.js`, `server/handlers/public/register.js` |
| D-123 | 2026-09-22 | **`/api/document` re-reviewed — the Stage 6 gate item — and D-034's framing CORRECTED by measurement.** D-034 treated the risk as guessing, multiplied across gyms. Measured: the code is 8 chars from a 32-char alphabet = **40 bits, 1.1 × 10¹²**, which is ~35 years at 1,000 req/s against one member, so **brute force is not the practical threat**. The practical threat is `select('*')`, which returns the whole `members` row — **`id_number` (SA ID), `face_descriptor` and `arcface_templates` (biometric)** — plus the PAR-Q health record, to anyone holding a permanent, reusable, non-expiring code, with **no rate limiting** (verified: `register.js` is the only public handler that has any). **Recommended: (1) explicit column list instead of `select('*')`, (2) per-gym rate limiting — both break nothing; (3) session-binding is the real fix and is a product decision.** NOT APPLIED — protected surface | The gate asked for a review, and the review contradicted the assumption behind the decision | [[17 - Open Questions]] §0.4, [[14 - Security and Privacy]] |
| D-122 | 2026-09-22 | **[[14 - Security and Privacy]] point 2 was WRONG and is corrected.** It claimed admin/member token separation "rests entirely on the `audience` claim". It rests on nothing — the admin verifier never passes an audience option, so the claim is set and ignored (F-1). Recorded as a correction rather than an edit, because the vault being confidently wrong about a security control is the failure mode `CLAUDE.md` §29 exists to prevent | The memory of record must be corrected out loud when it is found to be false | [[14 - Security and Privacy]] |
| D-121 | 2026-09-22 | **Document retention implemented (D-054).** `retention_until` is set when an application is REJECTED (the appeal window starts at the decision, not at the purge run); an approved gym's documents get **no expiry** while it trades. The nightly purge deletes **the storage object BEFORE the row** — the reverse order would leave a file holding somebody's ID that nothing points at, which is invisible; this order leaves a row pointing at nothing, which is visible and fixable. **A file that fails to delete does not have its row deleted.** Retention has its OWN switch (`PLATFORM_RETENTION_LIVE`), not billing's: one is about money, the other about personal data, and conflating them would mean enabling billing silently began deleting identity documents | The only job on the platform that deletes, so the care goes into the ordering | `platform/retention.js` |
| D-120 | 2026-09-22 | **Application documents upload straight to Supabase Storage via a signed URL; the bytes never enter the function.** Vercel caps a request body at ~4.5 MB and a scanned registration exceeds it routinely; a hand-written multipart parser is a classic vulnerability and a dependency for one form is worse; a function that never holds the bytes cannot leak them. **The SERVER decides the storage path** — from the verified application id, a fixed doc-type list and 16 random bytes. **The filename is never used in a path** (it is attacker text: `../../other-app/id.pdf` would be a cross-tenant write), and the returned path is re-checked with `pathBelongsTo()` before a row is written | Moving the upload off the function moves trust to the client, so the server keeps the naming | `platform/documents.js` |
| D-119 | 2026-09-22 | **Two login rules, by what the account can reach.** Platform staff reach EVERY gym, so 2FA is mandatory (D-077) and an account without it **cannot sign in at all** — half-finished setup is the situation the rule exists for. A gym owner reaches ONE gym, so a password is enough, and 2FA is honoured if they have turned it on. **Anything whose `kind` is not exactly `gym_owner` is treated as staff**, so an unrecognised value in the database produces the stricter rule. Owners land on `/platform/my-gym`, staff on the review queue | Requiring 2FA of everyone locked out every gym owner, since the signup form never sets up TOTP | `platform/router.js` |
| D-118 | 2026-09-22 | **The gym owner's platform page is NOT where they run their gym.** `/platform/my-gym` holds only what is between the owner and Yoyo Gyms: the application, its documents, the subscription, and a link out. Members, check-ins, payments and classes stay in the existing gym admin panel, which the owner enters as an `owner`-role user in their own gym (vault/07) and which this work does not touch (§32). Every query on that page is filtered by the SESSION's user id — there is no code path that can be pointed at another owner's gym | Two panels with two jobs; conflating them would drag platform concerns into protected surface | `platform/views.js`, `platform/deps.js` |
| D-117 | 2026-09-22 | **Architecture review of the four surfaces, 2026-09-22.** Verified: platform roles are a separate namespace from `owner/manager/reception/trainer`; `platform/` imports nothing from `server/` or `src/`; the existing 23 gym-admin routes are untouched; tenant isolation is covered by `tests/isolation.test.js`. **Three defects found and fixed (F-2, F-3, F-4) and one found and NOT fixed (F-1, protected surface).** F-3 was the serious one: a gym owner with a valid platform session could read every competitor's application. See [[17 - Open Questions]] §0.2–0.3 | The user asked for the architecture to be verified rather than assumed | `platform/router.js`, `platform/auth.js`, `platform/deps.js` |
| D-116 | 2026-09-22 | **The nightly drift report shares the billing cron, and its failure cannot lose the billing result.** One Vercel cron entry (`/api/platform/cron`, 03:00) runs billing then reconciliation, inside a `try` whose failure is reported as `drift: { ok: false }` alongside a complete billing report. A second cron entry is a second thing to configure and forget; and by the time reconciliation runs, billing has **already moved money** — losing the record of that because a read-only check failed afterwards is the worst possible ordering of the two facts | Fewer moving parts; money is recorded before anything else may fail | `platform/router.js`, `vercel.json` |
| D-115 | 2026-09-22 | **Schema drift reconciliation added; the project version is kept, not replaced.** Under D-096 a failed provision strands a **schema**, not a project, and the consequence is **different in kind**: a stranded project cost ~$10/month (a bill), a stranded schema costs ~nothing but may hold a member's name, phone number and ID document **with no gym record saying who is responsible for it** — a POPIA exposure, not a cost. Both functions are **given no way to execute SQL or drop anything**, so the "reports, never deletes" rule is structural rather than a promise in a comment | D-096 changed what provisioning leaves behind when it fails | `platform/reconciliation.js` |
| D-114 | 2026-09-22 | **The trial subscription is created during provisioning, as a numbered step.** A gym with no `platform_subscriptions` row is refused by `accessFor()` with a **402 on day one**. The gym itself is still saved as `pending`: a trial is a *subscription* state, not a live gym. ⚠️ **This exposes a conflict between D-049 (first payment activates a gym) and D-070 (30-day free trial)** — under D-049 a trialing gym can never serve traffic, which makes the trial unusable. **Raised as Q-46, not silently resolved** | Found while wiring billing; a decision conflict is the user's to settle | `platform/provisioning.js`, [[17 - Open Questions]] |
| D-113 | 2026-09-22 | **Permissions are read from the database on every request, never carried in the session cookie.** The platform JWT says *who you are*; it deliberately does not say *what you may do*. Revoking a role therefore takes effect immediately rather than whenever an 8-hour cookie happens to expire. Cost is one indexed query per page view on a panel used by one or two people | Revocation that takes 8 hours to apply is not revocation | `platform/router.js`, `platform/deps.js` |
| D-112 | 2026-09-22 | **Paystack webhooks: signature over the RAW BYTES, and 200 for events we ignore.** The body is read with `readRawBody()` and verified *before* it is parsed — `JSON.parse` then `JSON.stringify` does not reproduce the bytes signed (key order, unicode escaping, whitespace), so every genuine webhook would be rejected as a forgery. An unrecognised event, or one with **no reference**, returns 200 and does nothing: Paystack retries anything else forever, and **guessing which invoice a payment belongs to would mark the wrong gym paid** | Two failure modes that are silent until they are expensive | `platform/router.js`, `platform/billing.js` |
| D-111 | 2026-09-22 | **The double-charge guard is one invoice per gym per period, enforced by a unique index.** `platform_invoices.period_end` plus `platform_invoices_one_per_period_idx` means a cron that fires twice — a Vercel retry, a person pressing the button — is refused by **the database**, not merely by the application remembering to check. The invoice is also written **before** the charge is attempted: a process that dies mid-charge leaves an unpaid invoice (a question a human can answer) rather than money taken with no record of why | Billing that runs twice is the default failure of every cron | `platform/schema.sql`, `platform/billing-runner.js` |
| D-110 | 2026-09-22 | **Billing decides; the runner acts. `runBilling` is a DRY RUN unless told otherwise.** `platform/billing.js` is pure, takes `now`, touches nothing, and returns intents; `platform/billing-runner.js` carries them out and decides nothing. A dry run and a live run therefore make **identical decisions by construction**. The runner is **given no delete function** — suspension stops a gym, removing its data is a human decision (D-071). One gym's failure is caught per-gym, because letting it escape would leave every gym after it in an unordered list unbilled, and **the same gyms would be missed every night** | The money path is the one that must be provable without a network | `platform/billing.js`, `platform/billing-runner.js` |
| D-109 | 2026-09-22 | **A plan with no price is never charged and never suspended for non-payment.** Prices are NULL until the user sets them (D-058). Charging 0, crashing, or cutting a gym off over an amount **nobody ever set** are all wrong; billing simply does not run for that plan and says so in the report | Prices are deliberately unset and must stay safe to leave unset | `platform/billing.js` |
| D-108 | 2026-09-22 | **`platform/paystack.js` is a separate module from `server/lib/paystack.js`, and not only because of D-081.** They talk to **different Paystack accounts**: the gym's (the gym's money, the gym as merchant of record) and ours (gyms paying Yoyo Gyms). One shared module means one shared secret key, and the first bug is the platform billing a gym **through the gym's own account** — taking the gym's money and paying it back to the gym. Second difference: the gym helper takes **rands** and multiplies by 100; everything platform-side is already in **cents**, so this module takes cents and refuses a non-integer rather than rounding | Two accounts is a correctness boundary, not a style choice | `platform/paystack.js` |
| D-107 | 2026-09-22 | **G-1 (automated recurring member billing) stays ON HOLD.** The market treats it as standard and we removed it deliberately (D-015/D-018); members pay their gym directly. **Not revived** | User instruction 2026-09-22: "keep billing of member hold on" | `CLAUDE.md` §18.5 |
| D-106 | 2026-09-22 | **Feature keys and the route→feature map live in `shared/features.js`.** The platform *defines* which plan includes which feature; the gym app's routers *enforce* it — and D-081 forbids either importing the other. `shared/` is the existing common ground. **One definition, two readers**; duplicating it would guarantee drift | Boundary constraint made the location obvious | `shared/features.js` |
| D-105 | 2026-09-22 | **Gating WIRED: admin router + registration.** `enforceEntitlement()` in `api/admin/[...path].js` covers all 37 admin routes in one line; `allowsMemberRegistration()` in `public/register.js` enforces the member cap. ⚠️ **Both are completely INERT in single-gym mode** — no resolved gym means no plan, everything allowed, and the member-count query is not even run. **Existing deployments behave exactly as before**, which is asserted by the first two tests in `entitlements.test.js`. The opposite failure is also guarded: **a resolved gym with no features list gets NOTHING**, so a missing plan can never be mistaken for single-gym mode and hand a Basic gym the whole system. **145 tests pass** | Protected surface touched deliberately, per `CLAUDE.md` §18.4 which the user approved | `api/admin/[...path].js`, `server/handlers/public/register.js`, `server/lib/entitlements.js` |
| D-104 | 2026-09-22 | **Owner registration asks "what else does your gym need?"** — free text, optional, stored on the application and shown in the platform panel (`CLAUDE.md` §18.6). **Not a feature-request form: demand evidence.** Three gyms asking for the same thing outweighs any amount of speculation, and it costs one text box. Answers are read against the §18.5 gap list | User instruction | `CLAUDE.md` §18.6 |
| D-103 | 2026-09-22 | **Gap analysis written (`CLAUDE.md` §18.5): 10 market-standard features NOT built.** The two that matter: **G-1 automated recurring billing** — the market treats it as standard and **we removed it deliberately** (D-015), so gyms will ask; and **G-2 lead/prospect management** — the clearest genuine gap, since there is no concept of a prospect who has not joined. Others: member app (planned), marketing automation, POS/retail, member SMS, website builder, workout programming, multi-location, payroll. **It is a menu, not a backlog — nothing is built speculatively** | User asked what the market sells that is not built | `CLAUDE.md` §18.5 |
| D-102 | 2026-09-22 | **Plan gating built** (`platform/plans.js`), tests-first, **17 tests**. Tiers **nest** (asserted), core operation is in every tier (asserted), face recognition is Prime-only. Route→feature map covers **all 42 admin routes**; an **unmapped route fails closed** rather than becoming silently free. Blocked returns **402** naming the cheapest plan that includes it. **A downgraded gym keeps every existing member** — only new registrations stop, and `existingMembersAffected: false` is returned explicitly because it is the first thing an anxious owner asks. `ownerFacingPlan()` leads with **what members can do**, because that is what an owner is buying. **134 tests pass** | Implements `CLAUDE.md` §18 | `platform/plans.js` |
| D-101 | 2026-09-22 | **Enforcement: BLOCK and offer the upgrade.** Feature gating lives in the **API routers** (one fixed key map each — 42 admin routes gated in ONE file, never a check added to 76 handlers). Member limits are enforced in `register.js` and `members-import.js`, the only two places a member is created. Client-side hiding is **UX only**. A blocked feature returns **402**, not 403 — it is a billing state, not a permission error. **A downgrade never deletes data**; only new registrations are blocked | User choice; enforcement point follows the same one-seam principle as tenancy | `CLAUDE.md` §18.4 |
| D-100 | 2026-09-22 | ⚠️ **The pricing floor is GONE.** [[11 - Subscription Decisions]] said Basic must clear ~$10/month to cover Supabase. **D-096 (schema-per-gym in one free project) made the marginal cost of a gym approximately zero**, so that constraint no longer exists. Pricing is now a pure market decision. International rates are $75–200/month (≈R1,400–3,800); SA independent gyms are materially more price-sensitive, so pricing at international rates would be a strategic error | Recorded because the old floor is stated in note 11 and would otherwise mislead | [[11 - Subscription Decisions]], `CLAUDE.md` §18.3 |
| D-099 | 2026-09-22 | **The three plans are now DEFINED in `CLAUDE.md` §18**, from the feature inventory plus market research. **Core gym operation is in every tier** — registration, members, check-in, payments, catalog, branding, staff, QR, PDFs. **Medium adds** classes, trainers, messaging, standard reporting, progress, CSV. **Prime adds** face recognition and the door scanner, visitors/incidents, advanced analytics, bulk email, referrals, audit log. Member limits ~40 / ~150 / ~500. **Gating is deliberately light**: the market's loudest complaint is add-on gouging (a "$159" plan reaching $664), and Gymdesk competes explicitly on not doing it. **Face recognition maps onto the market's "access control" premium category**, making it the natural flagship of the top tier | Market evidence: two dominant models — by member count (Gymdesk, Mindbody, Zen Planner) and by feature tier (PushPress, TeamUp). Member count is the primary lever because it scales with the gym's own revenue | `CLAUDE.md` §18 |
| D-098 | 2026-09-21 | **Per-gym secrets (D-043 Infisical, D-069 free tier) are NOT NEEDED YET.** In schema mode every gym uses the platform's own project credentials, so there are no per-gym secrets to store. `gym_secrets` and the secrets-manager design stay in the schema and in [[12 - Database Architecture]] for **project mode** — a gym that later moves to its own Supabase account. **Fewer moving parts for now** | Falls out of D-096; recorded so nobody builds a secrets integration that nothing needs | D-043, D-069 |
| D-097 | 2026-09-21 | **Schema naming: `gym_<slug>`, not single letters.** GYM A … GYM Z caps at **26 gyms**; a slug has no ceiling and names the owner when you are reading the database at 11pm. ⚠️ **Each new schema must be added to `pgrst.db_schemas` and PostgREST reloaded** — one extra provisioning step, and **the reload briefly affects every gym on the project**, so it is done deliberately, not during busy hours | User is already using a `gym` prefix; extending it to a slug costs nothing now and is annoying later | `platform/provisioning.js` (pending) |
| **D-096** | 2026-09-21 | ⚠️ **TENANCY CHANGED: SCHEMA-PER-GYM in ONE shared Supabase free project.** Each gym gets its own Postgres schema (`gym_ironworks.members`). **Free**, no rewrite, stays on Vercel. **Implemented** — resolver updated, 9 isolation tests, 117 tests pass, single-gym mode untouched. **Supersedes D-095** (gym-owned accounts: manual onboarding, 7-day pausing) and **changes D-014 from PHYSICAL to LOGICAL isolation** — every gym now shares one database. **That is a real reversal of a decision made on POPIA grounds, accepted knowingly for cost.** A resolver bug is now a cross-gym data leak rather than a wrong answer, which is why the isolation tests matter more than before, not less. Practical ceiling: ~50 small gyms on 500 MB free | User decision: stay free, stay on Vercel, no Railway. The existing code already configured `db.schema`, so this was the cheapest possible path | `server/lib/tenancy.js`, [[06 - Tenant Architecture]], [[22 - Storage Migration Assessment]] |
| D-095 | 2026-09-21 | ⚠️ **EACH GYM USES ITS OWN SUPABASE ACCOUNT — the platform pays nothing for gym databases.** Returns to what `scripts/business-guide.js` §3 already described (*"ideally the gym's own project… you can manage it on their behalf"*). **This is a reversal with consequences that MUST be resolved before more is built — see Q-49.** It collides with **D-078** (automatic provisioning: you cannot create a project inside someone else's account without their credential) and with **D-012** (thousands of gyms in 24 months: an account each gym must create by hand is a manual step at every onboarding). It also exposes the gym to **free-tier pausing after 7 idle days**. **Not silently reconciled** | User decision: everything free except the app subscription. CLAUDE.md contains no free-tools rule — the source is the business guide | Q-49, D-078, D-012, `platform/provisioning.js` |
| D-094 | 2026-09-21 | ⚠️ **Platform sessions are COOKIE-based, not Bearer — which introduces CSRF, a risk the gym app does not have.** Server-rendered pages cannot hold a token in JavaScript, and a browser attaches cookies automatically to *any* request to the origin, including one triggered by another site. **Two defences, both required:** `SameSite=Strict` (the strong one) **and** a session-bound CSRF token on every state-changing form (the belt, which still holds if SameSite is ever relaxed for some flow). **A valid session is deliberately NOT sufficient for a POST** — there is a test asserting exactly that. Cookie is `HttpOnly`, `Secure`, `Path=/platform` so it never reaches a gym deployment | Direct consequence of D-091 (server-rendered HTML) | `platform/http.js` |
| D-093 | 2026-09-21 | **Going live waits for the Supabase answer.** No platform Supabase project is created yet — a project cap below target would reopen D-016/D-014 and change what gets built. Code and tests continue against injected fakes meanwhile | Avoids committing infrastructure before the architecture is confirmed | [[17 - Open Questions]] U-1 |
| D-092 | 2026-09-21 | **Reconciliation runs on a DAILY CRON plus an on-demand button.** ⚠️ **Not wired yet** — it needs the platform deployment, which waits on D-093. Recorded as a deployment task so it is not forgotten: an orphaned project costs ~$10/month silently until something runs this | Daily catches an orphan within 24h; the button is for investigating | `platform/reconciliation.js` |
| D-091 | 2026-09-21 | **Platform screens are SERVER-RENDERED HTML** (`platform/views.js`), no build step, no second Vite config, no React — the pattern proven next door in Telga. **Escaping is the whole security story**: every human-supplied value goes through `escapeHtml`, including staff input, and 5 of the 10 tests are XSS cases. No inline event handlers, so a strict CSP stays possible. **103 tests pass** | An internal panel for one or two people; it cannot import the gym app's components (D-081) and duplicating a design system would buy nothing | `platform/views.js` |
| D-090 | 2026-09-21 | **Application review flow built** (`platform/applications.js`), tests-first, 8 tests. Approve / reject / request-more-info, permission-gated, with a state machine that **refuses any decision on an already-decided application — approving twice would provision twice and bill twice**. Rejection **requires a reason** (D-061). Requesting information is deliberately **not** a decision (`decided_at` stays empty). The module is given `appendEvent` and **no update or delete**, so it cannot rewrite history. **Approval and provisioning are recorded as SEPARATE events** because they can diverge: a correct decision can be followed by a failed provision. **93 tests pass** | Stage 5 MVP scope (D-079) | `platform/applications.js` |
| D-089 | 2026-09-21 | **Recovery codes are hashed with SHA-256, not bcrypt** — deliberately. bcrypt's cost factor defends **low-entropy human-chosen passwords**; a recovery code here is ~50 bits of randomness, so brute force is infeasible regardless of hash speed and there is nothing to precompute. bcrypt at cost 12 for 10 codes made the suite take **two minutes** and bought no security. Comparison is constant-time | Security reasoning, not a speed shortcut — the speed was the symptom that prompted the review | `platform/auth.js` |
| D-088 | 2026-09-21 | **Recovery codes: 10, single-use, shown once at setup, stored as hashes only** (answers the 2FA lockout risk). Without them, losing the authenticator device means **permanent lockout from the platform that runs every gym** | The one account whose loss has no workaround | `platform/auth.js` |
| D-087 | 2026-09-21 | **TOTP implemented directly with `node:crypto`, no new dependency.** ~40 lines: HMAC-SHA1 over a 30s counter, dynamically truncated (RFC 6238/4226). **Validated against RFC 6238's own published test vectors**, and base32 against RFC 4648's — correctness checked against the specification, not against itself. Constant-time comparison; ±1 step of clock drift accepted. Platform session 8h with `audience: 'platform'` | Keeps the project at 10 runtime dependencies; `node:crypto` does the actual cryptography | `platform/auth.js` |
| D-086 | 2026-09-21 | **Supabase Management API client** (`platform/supabase-admin.js`): create, list keys, list projects, delete. `SUPABASE_MANAGEMENT_TOKEN` is a **platform-level secret** — it can create and delete every gym's database, so it never goes near `gym_secrets` and never reaches a browser. **`deleteProject()` refuses unless the caller passes `iUnderstandThisIsPermanent: true`**, so it cannot be invoked by reflex. API errors do not echo the response body, which can contain project identifiers | Endpoints verified from the Supabase Management API reference, not guessed | `platform/supabase-admin.js` |
| D-085 | 2026-09-21 | **Q-48 ANSWERED — a reconciliation job that REPORTS orphans** (`platform/reconciliation.js`), tests-first, 7 tests. Looks both ways: **orphans** (a billed project with no registry row) and **dangling** (a registry row pointing at a deleted project, meaning that gym is broken). **It is never given a delete function**, so it cannot destroy anything. Reports an estimated monthly waste figure, because the cost is the point. Findings are audited even on a clean run | Catches every case including failures the orchestrator never saw, and deletes nothing on its own | `platform/reconciliation.js` |
| D-084 | 2026-09-21 | **Provisioning orchestrator built** (`platform/provisioning.js`), tests-first, 8 tests. **Dry run is the DEFAULT** — calling it without `{dryRun:false}` creates nothing and costs nothing. Steps run in a fixed order, stop at the first failure, and the gym is registered `pending` not `active` (D-049). **The secret value never reaches the platform database** — enforced by a test that asserts the service key does not appear in the serialised row, since no DB constraint can express that invariant (D-022). **64 tests pass** | Provisioning spends real money; an accidental call must not | `platform/provisioning.js` |
| D-083 | 2026-09-21 | **Supabase enquiry drafted** at `platform/supabase-enquiry.md` — ready to send, with a table of what each possible answer means. Covers U-1 (project cap), U-4 (volume pricing), Management API rate limits, and whether they would advise against this pattern | Two open items rest on it, one of which can reopen D-016 and D-014 | [[17 - Open Questions]] |
| D-082 | 2026-09-21 | **Platform schema written as `platform/schema.sql`** — the 17 tables of D-022 as real, reviewable SQL, including the later additions (`gyms.latitude/longitude` D-065, `platform_subscriptions.grace_ends_at` D-026, `application_documents.retention_until` D-054). **Not executed anywhere.** Mirrors `db/schema.sql` in style, RLS posture and deletion policy | The approved design made concrete | `platform/schema.sql` |
| D-081 | 2026-09-21 | ⚠️ **SUPERSEDES D-045: the platform is built INSIDE this repository, under `platform/`, to be split out later.** The risk was stated when offered — it mixes platform work with the protected gym surface — and the user chose it for speed. **Mitigation, and it is binding: everything platform-side lives under `platform/`, and there are NO cross-imports in either direction.** `platform/` must not import from `server/` or `src/`, and the gym app must not import from `platform/`. Kept that way, the later split is a mechanical move rather than an untangling | User decision 2026-09-21, overriding the earlier separate-repository decision | D-045, repository layout |
| D-080 | 2026-09-21 | ⚠️ **Behaviour change recorded, not hidden: the Supabase env check moved from MODULE LOAD to FIRST CALL.** The original comment said *"fail loud at cold-start rather than silently mis-querying"* — deliberate. It had to move because a **platform deployment legitimately has no gym database of its own**; importing the module would have crashed it. A misconfigured single-gym deployment still fails loudly with the identical message, on its first query instead of at import | Required by platform mode; the spirit (fail loud, never mis-query) is preserved | `server/lib/supabase.js` |
| D-079 | 2026-09-21 | **Stage 5 MVP: applications and provisioning ONLY.** List applications, review documents, approve/reject, provision the gym. Subscriptions, finance, monitoring and audit views come later | It is the one thing nothing else can do, and it is what lets a real gym onboard | [[05 - Main Platform Admin Panel]] |
| D-078 | 2026-09-21 | **Provisioning is AUTOMATIC on approval.** Approving an application creates the Supabase project, loads schema and seeds, writes secrets to Infisical and inserts the registry rows | D-012 put thousands of gyms in scope, so provisioning must be hands-off. Approval is already the human checkpoint (D-048) | [[07 - Owner Workflows]] |
| D-077 | 2026-09-21 | **Platform owner login: email + password + 2FA.** Mirrors the proven gym-admin hardening (bcrypt, lockout, generic errors) plus a second factor. **2FA was deferred for gym staff but is required here** — this account approves gyms and reaches every gym's secrets | Highest-value account on the platform | [[13 - Authentication and Roles]] |
| D-076 | 2026-09-21 | ✅ **§32 APPROVED AND IMPLEMENTED — `getSupabase()` is request-scoped.** Built **tests first** as instructed: 7 isolation tests written, watched fail, then `server/lib/tenancy.js` implemented until green. Uses **`AsyncLocalStorage`**, so concurrent requests on one warm instance cannot overwrite each other **and all ~76 handlers are unchanged**. Single-gym mode is untouched and still the default. **56 tests pass, build succeeds** | User approval 2026-09-21, tests-first | `server/lib/supabase.js`, `server/lib/tenancy.js`, `tests/isolation.test.js` |
| D-075 | 2026-09-21 | **Stage 4 design delivered** ([[06 - Tenant Architecture]] §6d): three-cache resolution, failure modes that all fail closed, and the Stage 5 isolation test specified **before** the panel is built. ⚠️ **`getSupabase()` becoming request-scoped is protected surface (§32) and needs its own approved decision before any code is written** | Design only; nothing implemented | Stage 4 |
| D-074 | 2026-09-21 | **Q-37 reframed — there is no connection pool to exhaust.** Verified: `supabase-js` depends on `postgrest-js` with **no `pg`/TCP driver**; it speaks HTTPS to PostgREST. A client is a small object, not a database connection, and Postgres limits are handled server-side per gym project. **The real hot-path cost is the SECRET FETCH on cold start**, which makes Infisical's rate limits the thing to verify, not pooling | Checked the dependency tree instead of assuming a database driver | [[06 - Tenant Architecture]] §6d.1 |
| D-073 | 2026-09-21 | **Empty gym search returns the NEAREST gyms whatever the distance, with the distance shown.** The app never shows an empty screen | A member in a town with no gym seeing "closest is 80 km" is honest and still useful; an empty result looks broken | [[10 - Mobile App]] |
| D-072 | 2026-09-21 | **Supabase per-gym cost research (U-2) commissioned** — real per-project cost, the monthly floor for a small gym, and what each tier must charge to break even | Last thing blocking pricing (D-058 left tiers deliberately unpriced) | [[11 - Subscription Decisions]] |
| D-071 | 2026-09-21 | **Deprovisioning: warn, then DELETE 90 days after suspension.** ⚠️ **This REFINES D-027, it does not contradict it.** D-027's rule stands: *a billing event never destroys data* — suspension alone deletes nothing. What is added is an **end** to indefinite retention: 90 days suspended, with email warnings, then the gym's Supabase project is deleted. **Full worst case: 30-day trial + 90 days suspended = up to 120 days of compute for a gym that never pays a cent** | Without an end date, every gym that ever trialled stays on the bill forever | [[11 - Subscription Decisions]], [[07 - Owner Workflows]] |
| D-070 | 2026-09-21 | **Trial length: 30 days.** A full billing cycle before the gym pays | User choice. Doubles the carried hosting versus 14 days — see D-071 for the total exposure | [[11 - Subscription Decisions]] |
| D-069 | 2026-09-21 | **Secrets manager: INFISICAL, free tier. No paid monthly plan.** Answers D-067. Free cloud tier = **5 identities**, unlimited projects, unlimited secrets. Our architecture needs ~2 machine identities (D-016), so it fits. ⚠️ **Two honest caveats recorded rather than buried:** (a) **the free tier has a ceiling at 5 identities** — the exit when we hit it is **self-hosting the MIT core** (free software, but needs a host and someone to run it), not a surprise bill; (b) **depending on a vendor's free tier for a load-bearing production dependency is itself a risk** — free tiers change. The MIT self-host option is the mitigation, and is precisely why Infisical beat AWS | User: no expensive monthly fees. The per-identity model stays flat at any gym count *because of* the resolver design | [[12 - Database Architecture]] §7b |
| D-068 | 2026-09-21 | **Capacitor spike DEFERRED to Stage 8.** Keep the stage discipline; the mobile risk is real but blocks nothing yet, and Telga's evidence is reasonably strong | User choice | [[19 - Implementation Phases]] |
| D-067 | 2026-09-21 | **Secrets-manager vendor: Claude to research and recommend.** Compare Infisical / Doppler / AWS Secrets Manager / HashiCorp Vault on cost at thousands of secrets, rotation, audit, and serverless access from Vercel. Recommendation to be brought back for approval — **not adopted unilaterally** | User delegated the research, not the decision | D-043, Stage 4 |
| D-066 | 2026-09-21 | **Branch `docs/yoyo-gyms-second-brain` merged to `main`.** Not pushed to GitHub | Tests, build and vault validation all pass; no gym is live; CI should run on the default branch and future work should start from a clean base | repository |
| D-065 | 2026-09-21 | **Gym search = name search PLUS nearby gyms by device location.** ⚠️ **Three consequences:** (a) `gyms` needs **latitude / longitude** — added to the approved-but-unbuilt schema now, while it is free; (b) the app needs a **location permission**, which is an app-store privacy disclosure and a POPIA processing purpose; (c) gym **coordinates must be captured during owner onboarding** (D-047 collects proof of premises but no coordinates) | Makes the platform genuinely useful for discovery rather than only routing. Search is now the primary way members find a gym (D-036) | [[12 - Database Architecture]], [[07 - Owner Workflows]], [[15 - Store Compliance]] |
| D-064 | 2026-09-21 | **`npm run docs:validate` added and wired into CI.** Checks frontmatter keys, wikilink resolution **through aliases** (D-006), orphans, dangling `D-###` references, and encoding damage. Errors fail the build; orphans and unknown question refs are warnings. Found one real gap on its first run (note 01 missing `updated`) and was negative-tested against a deliberately broken note | Stale and self-contradicting vault content was hand-fixed **five times** on 2026-09-21. The vault is the memory of record, so drift should fail a build, not wait to be spotted. Pattern taken from Telga's `validate-vault.mjs` (D-052) | `scripts/validate-vault.mjs`, `.github/workflows/ci.yml` |
| D-063 | 2026-09-21 | ⚠️ **Consequence of D-059 to design for: a free trial with no payment method means YOU carry the Supabase compute cost for every trial gym that never converts.** Provisioning happens at approval (D-049), and Supabase bills dedicated compute per project — so an abandoned trial is a real monthly bill, not a rounding error. Needs a trial-expiry job that **suspends and deprovisions abandoned trials** (never a paying gym's data — D-027) | Falls out of combining D-049, D-059 and per-project billing | [[11 - Subscription Decisions]], U-2 |
| D-062 | 2026-09-21 | **Q-20 is MOOT under D-053.** Capacitor wraps the existing web app, which calls `/api/*` exactly as the browser does. **No Supabase client ever reaches the device**, so "RLS policies must exist before a Supabase client reaches a mobile app" never triggers. The default-deny posture holds unchanged | The load-bearing invariant in [[14 - Security and Privacy]] survives the mobile app intact | [[14 - Security and Privacy]] |
| D-061 | 2026-09-21 | **Q-10b: reject with a reason; the owner may reapply.** No formal appeal process. Rejected applications' documents are purged after the ~90-day window (D-054) | Keeps the process light at current volume while leaving the door open to a fixed application | [[07 - Owner Workflows]] |
| D-060 | 2026-09-21 | **Q-08 / Q-14: gym subscriptions are bought ON THE WEB ONLY, never in the app.** Paystack (D-020). The app sells nothing. ⚠️ **Trap to design around:** app stores also restrict *linking or steering* to an external purchase from inside the app — "web only" is not automatically safe, and the app's wording must be checked against current store rules at Stage 10 | Avoids 15–30% store commission on a B2B product | [[11 - Subscription Decisions]], [[15 - Store Compliance]] |
| D-059 | 2026-09-21 | **Q-06: free trial, NO payment method required.** The gym is approved, provisioned and fully usable free for a trial period; payment is required before the trial ends. Consistent with D-049 (approve → provision → pay) | Lowest friction for a hands-on sales process. ⚠️ See D-063 for what it costs | [[11 - Subscription Decisions]] |
| D-058 | 2026-09-21 | **Q-04: keep THREE tiers gated on active member count; prices set later as DATA.** Shape locked (Basic / Medium / Prime), numbers deliberately not. `platform_plans.max_active_members` and `price_cents` already exist in the approved schema | Pricing needs the real per-gym Supabase cost first (U-2). `CLAUDE.md` §18: never hard-code limits or prices | [[11 - Subscription Decisions]] |
| D-057 | 2026-09-21 | **Consequence of D-056 worth naming: removing member payments (D-015) already cleared the hardest multi-country blocker.** Per-country payment gateways for *member* fees would have been the worst part of going multi-country. Gyms now collect locally by their own means, so currency becomes display and record-keeping only. **What remains:** national ID validation, per-jurisdiction contract and privacy text, and whether the platform's billing provider operates in each target country (**must be VERIFIED, not assumed**) | Fell out of an unrelated decision; recorded so nobody re-solves it | [[11 - Subscription Decisions]], [[14 - Security and Privacy]] |
| D-056 | 2026-09-21 | **Q-18: DESIGN FOR MULTIPLE COUNTRIES FROM THE START.** Country is a first-class concept on the gym registry (`gyms.country` already in the approved schema). ⚠️ **Scope expansion** — the existing registration flow is South-Africa shaped: SA ID Luhn validation, ZAR pricing, POPIA consent, CPA contract text | User decision 2026-09-21. MuleSoo already operates in Ethiopia via Telga | [[12 - Database Architecture]], [[14 - Security and Privacy]], [[15 - Store Compliance]] |
| D-055 | 2026-09-21 | **Q-16: member face templates are DELETED when the member leaves**, or on request. Members may re-enrol at any time (already supported). The existing FK cascade on member delete already implements the mechanism | First written biometric retention policy. Must be disclosed at app-store submission | `members.face_templates`, `arcface_templates` |
| D-054 | 2026-09-21 | **Q-12: application-document retention — minimum data, minimum time.** Rejected applications purged after a short appeal window (~90 days); an approved gym's documents kept while it trades and deleted a set period after it leaves. `application_documents.retention_until` already exists in the approved schema | Two of the four documents are identity documents, making this a POPIA obligation | [[07 - Owner Workflows]], [[12 - Database Architecture]] §4.3 |
| D-053 | 2026-09-21 | **Q-13 CLOSED — the mobile app uses CAPACITOR**, as Telga does: a shell wrapping the existing web app, shipped to both stores. Not React Native, not Flutter. **Nothing is reimplemented** — the admin panel, the 38-step chatbot and the member portal are wrapped, not rebuilt, and `face-api` + `jsqr` keep running on-device in the webview (satisfying D-042 with no native ML work) | Telga proves the pattern in-house; the existing app is already React and already a PWA | [[10 - Mobile App]] |
| D-052 | 2026-09-21 | **Q-41 ANSWERED — Telga is a REFERENCE, not a dependency.** MuleSoo's other product (merchant airtime vending, Ethiopia), inspected read-only 2026-09-21. Yoyo Gyms should work *the same way*: a Capacitor shell wrapping the server's own screens. **Nothing in Telga was modified, and Yoyo Gyms takes no runtime dependency on it.** D-024 stands | User: *"go scan look read how it works telga is then build, do not change or touch telga system, just look"* | [[10 - Mobile App]] |
| D-051 | 2026-09-21 | **Correction: Telga is ANDROID-ONLY today.** `apps/mobile/ios` does not exist; all scripts are `cap sync android` / `gradlew`. Capacitor supports iOS but Telga has not shipped it. **iOS is unproven ground for both products** and must not be treated as demonstrated | Verified by inspection rather than assumed from "android and iphone" | [[10 - Mobile App]], [[15 - Store Compliance]] |
| D-050 | 2026-09-21 | **Q-24 / Q-12(storage): application documents live in Supabase Storage in the PLATFORM project.** Never in a gym's database. `application_documents.storage_ref` points at it; access via signed URLs. ⚠️ **Retention PERIOD still undecided** — see Q-12 | Already in the stack, with access rules and signed URLs built in; keeps documents beside the registry that references them | [[12 - Database Architecture]] §4.3 |
| D-049 | 2026-09-21 | **Q-11: approve → create tenant → THEN require payment.** The gym is provisioned on approval but stays `pending`/trial until the first subscription payment, then becomes `active` | The owner can set the gym up before paying, and provisioning only ever happens for vetted applicants | [[07 - Owner Workflows]], `gyms.status` |
| D-048 | 2026-09-21 | **Q-10: every gym application is reviewed MANUALLY by the platform owner. No published SLA** for now | At early volume this is realistic, and it is the strongest guard against fake gyms appearing in app search — which matters more under D-036, where search *is* the discovery mechanism | [[07 - Owner Workflows]] |
| D-047 | 2026-09-21 | **Q-09: required application documents — all four.** Business registration (CIPC) · owner's ID or passport · proof of premises · tax clearance / VAT number. ⚠️ Two of these are **identity documents**, which carry POPIA retention duties and make Q-12 (retention period) a real obligation, not paperwork | User choice 2026-09-21 | [[07 - Owner Workflows]], `application_documents.doc_type` |
| D-046 | 2026-09-21 | **Consequence of D-044 — the platform panel never holds gym database credentials.** Only the gym-serving application (the resolver) and the migration orchestrator need them. The platform admin panel, now a separate deployment (D-045), can run with **no access to any gym's data at all**. This materially shrinks the blast radius of the shared-application trust boundary flagged in [[14 - Security and Privacy]] | Falls out of "no support access, ever" — if the panel can never read gym data, it never needs the keys | [[12 - Database Architecture]], [[14 - Security and Privacy]] |
| D-045 | 2026-09-21 | **Q-22 / Q-30: the platform lives in a SECOND repository** (e.g. `yoyo-gyms-platform`) with its own deployment. The gym system stays untouched; shared code is copied or published as a small package | Platform work cannot accidentally disturb the protected surface | [[19 - Implementation Phases]] |
| D-044 | 2026-09-21 | **Q-31: the platform owner can NEVER open a gym's admin panel.** No impersonation, no support access, no exceptions. Support is done by asking the owner what they see | Strongest privacy position and the cleanest promise to gyms: nobody at Yoyo can see your members. Fully consistent with the gym being POPIA responsible party (D-014) | [[05 - Main Platform Admin Panel]], [[14 - Security and Privacy]] |
| D-043 | 2026-09-21 | **Q-36: per-gym secrets live in a DEDICATED SECRETS MANAGER** (Infisical / Doppler / AWS Secrets Manager / Vault — specific vendor still to pick). `gym_secrets` continues to hold **references only** (D-022) | Built for versioning, rotation and an audit trail of every access; Vercel env vars cap at 64 KB total per deployment and cannot hold thousands of gyms | [[12 - Database Architecture]] §4.4, §7 |
| D-042 | 2026-09-21 | **Q-46 RESOLVED — face matching is SPLIT.** **1:1 member sign-in → on-device** (or the phone's own biometric unlock guarding a stored credential, per `CLAUDE.md` §20), matched only against that member's own data. **1:N door/turnstile → server-side ArcFace** (D-039). **A gym's member face gallery never leaves the server** | 1:N on-device would mean shipping every member's biometric template to a phone — special personal information under POPIA | [[14 - Security and Privacy]], [[10 - Mobile App]] |
| D-041 | 2026-09-21 | **Q-03 answered: SEPARATE member identity per gym.** A person who joins two gyms is two unrelated member records. No platform-level member identity, and **no change to the existing member login** — the member picks their gym in the app first, then signs in with membership number + phone exactly as today | User choice 2026-09-21. Matches the per-gym database decision (D-014) and keeps the protected login surface untouched | [[08 - Member Workflows]], [[13 - Authentication and Roles]] |
| D-040 | 2026-09-21 | **App scope is NOT re-litigated — `CLAUDE.md` §15 already specifies it.** Two areas, Gym Owner and Gym Member, with the member capabilities listed and the owner side phased ("must *eventually* support"). Read §15 before asking about app structure | User correction 2026-09-21: the architecture is already written down | [[10 - Mobile App]] |
| D-039 | 2026-09-21 | **Deploy the ArcFace service** (`face-service/`) — reverses the earlier deferral. The app needs server-side face matching. ⚠️ **In tension with D-038b (on-device matching) — see Q-46** | User decision 2026-09-21 | `server/lib/faceservice.js`, `FACE_SERVICE_URL` |
| D-038 | 2026-09-21 | **Mobile framework: ONE cross-platform codebase shipped as real store apps.** Not a PWA; not two native codebases. Specific framework (React Native vs Flutter) still to confirm — **React Native recommended**, since the entire existing stack is React and `shared/pricing.js` + `shared/countries.js` are plain JS that could be reused directly | User choice 2026-09-21 | [[10 - Mobile App]] |
| D-037 | 2026-09-21 | **Refunds REMOVED entirely** (answers Q-45). `PATCH /api/admin/payments` deleted, plus the Refund button in `PaymentsAdmin.jsx`. Gyms handle refunds outside the software, consistent with members paying outside it. The broken `refunded_amount` write is gone rather than fixed. **Implemented; 49 tests pass, build succeeds** | User decision. Fixing it would have meant the project's first migration for a path the product no longer wants | `admin/payments.js`, `PaymentsAdmin.jsx` |
| D-036 | 2026-09-21 | **Q-02 answered — NO per-gym subdomains.** Each gym has a **name** and **its own QR code**. Members find their gym **inside the Yoyo app**: scan the gym QR, or search the gym name against the platform registry, which is populated when the gym registers through the platform admin panel. Gym identity travels as a request parameter, not a hostname | User instruction 2026-09-21 | [[06 - Tenant Architecture]] §6c, [[09 - QR-Code Architecture]], [[10 - Mobile App]] |
| D-035 | 2026-09-21 | **The tenant-resolution rule is restated, not weakened.** With no hostname to read, a client may **name** a gym but may never be **granted** anything on that name alone: the server resolves the name, then verifies credentials against that gym's own database and its own `JWT_SECRET`. Isolation never rested on hiding which gym was asked for | Consequence of D-036 | [[06 - Tenant Architecture]] §6c |
| D-034 | 2026-09-21 | ⚠️ **Q-19 UPGRADED to a blocking pre-launch item.** `/api/document` authorises on membership number + verification code with no session. Under one gym that is a two-secret bearer token; once any client can aim it at **any** gym (D-036), the guessing surface multiplies by the number of gyms. Must be session-bound, per-gym rate-limited, or both, **before launch** | Direct consequence of app-based routing | [[14 - Security and Privacy]], [[17 - Open Questions]] |
| D-033 | 2026-09-21 | **Rate limiting must become PER GYM.** Under D-036 every public gym-scoped endpoint (`/api/catalog`, `/api/content`, `/api/register`) is reachable for every gym from one app, so a global limiter lets one gym's traffic starve another's | Direct consequence of app-based routing | `server/lib/ratelimit.js` — Stage 4 |
| D-032 | 2026-09-21 | **Two pre-existing bugs found while implementing.** (a) `activation.js` wrote `paystack_auth_code` to the **`payments`** table — that column exists only on `memberships` — and the error was **never checked**, so it failed silently and the payment row never actually became `received`, defeating the idempotency guard. **Fixed** as part of the rewrite. (b) `admin/payments.js` PATCH writes **`refunded_amount`**, a column that exists on no table, so **refunds error out**. **NOT fixed — outside this change's scope**, reported for a separate decision | Found by checking columns against `db/schema.sql` rather than trusting the code. Neither has ever bitten anyone because no gym is live | Q-45 |
| D-031 | 2026-09-21 | ✅ **MEMBER-PAYMENT REMOVAL IMPLEMENTED.** 9 files deleted, 11 changed, 1 added, 7 tests added. **49 tests pass, production build succeeds.** Sequence followed as designed: activation wired and verified **first**, removals second | User approval 2026-09-21 ("do the whole removal") | [[21 - Member Payment Removal Design]] |
| D-030 | 2026-09-21 | **`retry-suspend.js` → `suspend-overdue.js`.** Paystack retry is gone, so "overdue" is now redefined as *billing date passed beyond a grace window with no captured payment*. Cron route renamed `/api/cron/retry-suspend` → `/api/cron/suspend-overdue` | The old trigger (`payments.status='failed'`) can no longer occur — nothing sets it — so keeping it would have been dead code that never fires | `daily.js`, `api/cron/[...path].js` |
| D-029 | 2026-09-21 | **Q-44: the member portal still shows the outstanding amount, with no online payment.** `PayBalance` replaced by `OutstandingNotice` — shows the figure, directs the member to reception | Visibility is useful and cuts front-desk questions | `MemberPortal.jsx` |
| D-028 | 2026-09-21 | **Q-42: automatic member suspension becomes a per-gym setting, OFF by default.** `settings` key `billing_rules` = `{ auto_suspend_overdue, grace_days }` | User: payment matters are each gym's own decision. Default-off means no gym inherits a policy it did not choose | `suspend-overdue.js` |
| D-027 | 2026-09-21 | **Suspension never deprovisions.** A suspended gym keeps its Supabase project, its database and all its data. Suspension removes *platform access*, nothing else. Reactivation must be a status change, never a re-provision | User: *"we dont delete data"*. Also the POPIA-safe position — the gym remains responsible party for member data that must not be destroyed by a billing event | [[12 - Database Architecture]], [[07 - Owner Workflows]] |
| D-026 | 2026-09-21 | **Gym subscription dunning policy (answers Q-07):** gym fails to subscribe or a subscription payment fails → **2-day warning** → **suspend** → **no platform access**, but **data is retained, never deleted**. Reversible on payment | User instruction 2026-09-21 | [[11 - Subscription Decisions]]; needs a grace-window field in `platform_subscriptions` |
| D-025 | 2026-09-21 | **Q-43 answered: platform billing reuses the existing Paystack account and keys.** The credentials currently in `.env` become the *platform's*; they are not retired. ⚠️ Consequence: those keys must be **moved**, not deleted, when the member-facing payment path is removed, and they become a **platform-level** secret — never stored in `gym_secrets` (D-022) | User choice 2026-09-21 | [[21 - Member Payment Removal Design]] §2.4 |
| D-024 | 2026-09-21 | **Telga is NOT part of the architecture.** Q-41 stays open; Telga is recorded as unresolved and **not a confirmed dependency**. No note, table, field or design element references it | User instruction 2026-09-21 | [[17 - Open Questions]] |
| D-023 | 2026-09-21 | **Vault extended by one note**: `21 - Member Payment Removal Design`, beyond the 21 notes of `CLAUDE.md` §28 | The user asked for a *separate* documentation-only design for the payment-removal slot; folding it into note 03 would have buried a full change design inside a change-control policy note | [[21 - Member Payment Removal Design]] |
| **D-022** | 2026-09-21 | ✅ **STAGE 3 GATE CLOSED — the platform data model is APPROVED as designed** ([[12 - Database Architecture]] §4). Approved conditions, all binding: separate platform Supabase project · platform schema holds **no member data** · the existing 24 tables remain physically separate and untouched · platform stores registry, applications, documents, connections, secret references, subscriptions, invoices, audit, roles and migration records · **`gym_secrets` stores references only, never values** · tenant resolution **fails closed** · **no default-gym fallback** · platform roles and gym roles stay separate · platform Paystack credentials are platform-level · `platform_invoices` never contains member payment records · `server/lib/paystack.js` remains available · D-020 and D-021 approved. **Tables are NOT created; no migration exists** | User approval 2026-09-21 | Stages 4+ unblocked |
| D-021 | 2026-09-21 | **`server/lib/paystack.js` must NOT be deleted during the payment removal.** D-018 removes the *member-facing* Paystack flow; D-020 then needs the same Paystack primitives **platform-side** for gym subscriptions. Keep the library, delete the member payment path | Deleting and rewriting the same integration weeks later wastes work and loses the webhook-hardening (HMAC + independent re-verify) already proven in `payments/webhook.js` | [[03 - Protected Existing Functions]] |
| D-020 | 2026-09-21 | **Platform billing provider: Paystack**, for **gym→platform subscriptions only**. No member ever appears in `platform_invoices`. Platform Paystack credentials are a platform-level secret, never stored in `gym_secrets` | User instruction 2026-09-21. Answers Q-40 | [[11 - Subscription Decisions]], [[12 - Database Architecture]] §4.5 |
| D-019 | 2026-09-21 | **No gym is live in production yet.** The payment removal can therefore be a **clean deletion** — no feature flag, no staged cutover, no in-flight-transaction migration, no risk to real money | User confirmation 2026-09-21 | Removes the largest risk from D-015 |
| D-018 | 2026-09-21 | **Payment removal scope: remove the online card path, KEEP payment tracking.** Out: Paystack, `PaymentScreen`, `PaymentCallback`, member self-pay, `cron/billing.js`, `cron/retry-suspend.js`. **Stays**: `payments` table, manual cash/EFT capture, AR aging, dunning, receipts, and the per-gym `plans` / `addon_services` catalog. ⚠️ **Inferred** from the user's words *"allow each gym to make its own plan, I am not responsible for each gym plans and things related"* combined with D-017, which requires manual capture to exist. **Confirm before implementing** | Each gym defines its own plans, prices and payment arrangements; the platform neither dictates them nor carries responsibility for them. Gyms still need to know who has paid | [[03 - Protected Existing Functions]] |
| D-017 | 2026-09-21 | **Q-34 answered: recording a manual cash/EFT payment activates the member.** Staff capture a payment → member and membership become `active`, mirroring `activatePayment()` without Paystack | Keeps "paid = active" as the rule, so the membership lifecycle, arrears and reporting continue to mean something | `admin/payments.js`, `lib/activation.js` |
| D-016 | 2026-09-21 | **Q-01 DECIDED — Model C: one shared application deployment + one Supabase project per gym + gym resolution injected at `getSupabase()` + a per-gym secrets store + an orchestrated migration runner.** ⚠️ **Accepted risk:** the user approved outright rather than pending **U-1** (maximum Supabase projects per organisation, still undocumented). If a ceiling exists below the target, this decision and D-014 must both be revisited | The only shape satisfying every fixed constraint: physical data isolation (D-014), thousands of gyms (D-012), no Vercel repo or deploy ceiling, instant rollout, and a near-untouched protected surface. The graph showed all ~76 handlers reach the database through `getSupabase()` alone, so gym resolution has exactly one insertion point | **Unblocks Stages 3–10** |
| D-015 | 2026-09-21 | **Member payments to be REMOVED from the product.** Members will not pay through this system at all; each gym collects member fees by its own means, outside the software. ⚠️ **Approved in principle — implementation NOT started.** This changes the protected surface (`CLAUDE.md` §32), so the full impact analysis in [[03 - Protected Existing Functions]] §"Pending approved change" must be read and the **activation gap (Q-34) resolved** before any code moves | User decision 2026-09-21. The platform bills gyms only; member↔gym money is out of scope | `payments/*`, `paystack.js`, `activation.js`, PaymentScreen, PaymentCallback, `member/pay.js`, `cron/billing.js`, `cron/retry-suspend.js` |
| D-014 | 2026-09-21 | **Each gym must have its own database.** Physical data isolation is non-negotiable. **Rules out Model B** | Member data includes health (PAR-Q, injuries, medical aid) and biometric face templates — special personal information under POPIA. User will not pool it | [[06 - Tenant Architecture]] |
| D-013 | 2026-09-21 | **Revenue model: flat subscription from gyms only.** The platform never handles, routes or sees member money, and takes no share of it | Keeps MuleSoo out of the money flow and out of financial liability; simplest billing | [[11 - Subscription Decisions]] |
| D-012 | 2026-09-21 | **Scale target confirmed: thousands of gyms within 24 months — a real plan, not an aspiration.** Consequence accepted: the documented 1–2 hour manual onboarding runbook cannot survive; provisioning **must** become automated via the Supabase and Vercel management APIs | User decision 2026-09-21, after being shown the manual-runbook maths | [[06 - Tenant Architecture]], [[07 - Owner Workflows]] |
| D-011 | 2026-09-21 | **Graphify runs without `--obsidian`.** The graph is a retrieval layer in `graphify-out/`; it never writes into `vault/` | The 21 curated notes must not be overwritten by one-file-per-node output | Vault integrity |
| D-010 | 2026-09-21 | **`graphify-out/` is git-ignored** | Derived data, regenerable from `vault/` + source with one command; `graph.json` alone is 2.3 MB | Repository size |
| D-009 | 2026-09-21 | **Q-23 answered: project documentation IS committed to Git.** `vault/`, `CLAUDE.md`, `.gitignore` committed on `docs/yoyo-gyms-second-brain`. **Excluded**: `.obsidian/` (vendored plugin code + machine layout + `remotely-save` credentials), `.smart-env/` (private embeddings) | The second brain must be durable, reviewable and available to future sessions; app state and credentials must not be | Version control |
| D-008 | 2026-09-21 | Notes about **future** work (04, 05, 06, 07, 10, 11, 15) are marked `status: requirements-only` / `provisional-only` / `undecided` in frontmatter and state explicitly that they contain no decisions | Prevents a later session mistaking a written requirement for an approved design — the failure mode `CLAUDE.md` §29 warns about | Notes 04–15 |
| D-007 | 2026-09-21 | **Vault structure confirmed** as the 21 notes of `CLAUDE.md` §28, in `vault/`, all carrying YAML `aliases:` and linking back to 00 / 01 / 17 / 18 | Stage 2 structure approved by the user; Graphify may run against it once the user authorises | Whole vault |
| D-006 | 2026-09-21 | Vault notes use **numbered filenames** (`01 - …`) per `CLAUDE.md` §28, **plus a YAML `aliases:`** entry carrying the bare title, so both `[[01 - Existing Yoyo Gym Audit]]` and `[[Existing Yoyo Gym Audit]]` resolve | §27 link names and §28 filenames conflicted; aliases satisfy both without renaming either | All vault notes |
| D-005 | 2026-09-21 | Vault notes live in **`vault/`** at the repository root, not scattered at root level | Keeps 21 planned notes out of the source tree; Obsidian resolves `[[links]]` vault-wide regardless of folder. Renameable later at no cost | Vault structure |
| D-004 | 2026-09-21 | **Proceed to Stage 2 (audit note) before Stage 1 (tenancy)** — a deliberate reordering of `CLAUDE.md` §34 | The user wants the existing system documented before architecture is chosen, so the tenancy decision is made against written evidence rather than recollection | [[19 - Implementation Phases]] |
| D-003 | 2026-09-21 | The **repository inspection of 2026-09-21 is the source of truth** for existing single-gym behaviour, recorded in [[01 - Existing Yoyo Gym Audit]] | The prior audit is gone; code is the only remaining primary evidence | All future work |
| D-002 | 2026-09-21 | The previous single-gym discovery result is **permanently absent**. Do not reconstruct or fabricate it | Searched repo, vault, git history (added and deleted files) and the home directory — it does not exist on this machine | `CLAUDE.md` §29 |
| D-001 | 2026-09-21 | **`CLAUDE.md` v2 approved as written**, after three verified corrections (24 tables not 25; 23 guarded routes not 24; store compliance is Stage 10 not Stage 9) | Errors originated in the discovery report and would have become source-of-truth defects | `CLAUDE.md` §6, §8, §19 |

## Standing rules carried from `CLAUDE.md`

- **Memory of record** — this vault plus Graphify, never chat history (§26)
- **Failure protocol** — investigate from notes, logs and code; never guess (§27.1)
- **Stage gating** — one stage open at a time; the gate is the user's word (§34)
- **Protected surface** — nothing in [[03 - Protected Existing Functions]] changes without an
  explicit approved decision recorded here (§32)

## Rejected ideas

*(none yet — record rejections here with the reason, so they are not re-proposed)*

## Related

[[00 - Project Purpose]] · [[01 - Existing Yoyo Gym Audit]] · [[17 - Open Questions]] ·
[[19 - Implementation Phases]] · [[20 - Change History]] · [[03 - Protected Existing Functions]]
